//Buscar un producto por NOmbre

/*Dado un arra productos, buscar un producto por su nombre yu mostrar sus detalles. si no existe,
mostrar "No encontrado"*/

const productos = [
    { id: 1, nombre: "Laptop", precio: 1000 },
    { id: 2, nombre: "Mause", precio: 20 }
];

for (let i = 0; i < productos.length; i++) {
    console.log(productos[i]);
    if (productos[i] === productos.nombre) {  
    }
}